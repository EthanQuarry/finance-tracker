import { Card, CardHeader, CardDescription } from "./ui/card";

export default function IncomeClient({getIncome, postIncome}) {
    return (
        <Card>
        <CardHeader></CardHeader>
        <CardDescription></CardDescription>
      </Card>
    )
}